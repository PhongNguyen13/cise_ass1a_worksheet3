name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "cise-heroku-app" #Must be unique in Heroku
          heroku_email: "qtc0531@autuni.ac.nz"
          appdir: "cise_ass1a_worksheet3" # <- This will point to the api folder in your project
          procfile: "web: npm start"
